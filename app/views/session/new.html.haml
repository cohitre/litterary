#main-content
  %h1 Login
  = form_for :user, :url => login_path do |f|
    %p= f.label_text :login
    %p= f.label_password :password
    %input.primary{:type=>"submit", :name=>"login", :value=>"Login &#187;"}
